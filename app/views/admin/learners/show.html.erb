<h2>学習者詳細</h2>

ニックネーム</br>
<%= @learner.name %>さん</br>

ステータス
<% if @learner.is_active == true %>
    有効会員</br>
  <% else %>
    退会済</br>
  <% end %>
現在のレベル　未実装</br>
累計獲得ポイント　p</br>

<%= link_to "学習者情報の編集", edit_admin_learner_path(@learner.id) %>

<%= @learner.name %></br>さんが作成した最新問題</br>
<table class="table table-hover table-inverse">
  <tr>
    <th>教科</th>
    <th>問題</th>
    <th></th>
  </tr>
  <% @created_problem.each_slice(5) do |row| %>
    <% row.each do |created_problem| %>
    <tr>
      <td><%= created_problem.subject.subject_name %></td>
      <td><%= created_problem.problem %></td>
      <td><%= link_to "詳細", admin_createdproblem_path(created_problem.id) %></td>
    </tr>
    <% end %>
  <% end %>
</table>