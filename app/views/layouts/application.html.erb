<!DOCTYPE html>
<html>
  <head>
    <title>StudyZone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="bg-info h-100">
      <nav class="navbar navbar-expand-lg">
        <div class="container">
          <nav class="navbar-brand">
            <% if admin_signed_in? %>
              <%= link_to "Study Zone", "/admin", class:"nav-link h2 text-primary" %>
            <% elsif learner_signed_in? %>
              <%= link_to "Study Zone", "/learner", class:"nav-link h2 text-primary" %>
            <% else %>
              <%= link_to "Study Zone", "/", class:"nav-link h2 text-primary" %>
            <% end %>
          </nav>
          <div class="text-center">
            <% if admin_signed_in? %>
              管理者　ログイン中
            <% elsif learner_signed_in? %>
              学習者　ログイン中
            <% else %>
              積極的なインプット・アウトプットを支援するサイトです！</br>
              ポイントをたくさんゲットしてランキング上位を目指しましょう！
            <% end %>
          </div>
          <ul class="navbar-nav ml-auto flex-row">
            <% if admin_signed_in? %>
              <li class="nav-item"><%= link_to "マイページ", admin_items_path, class:'btn btn-outline-secondary', role:'button' %></li>
              　<li class="nav-item"><%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class:'btn btn-outline-secondary', role:'button' %></li>
            <% elsif learner_signed_in? %>
              <li class="nav-item"><%= link_to "マイページ", customers_my_page_path, class:'btn btn-outline-secondary', role:'button' %></li>
              　<li class="nav-item"><%= link_to "ログアウト", destroy_learner_session_path, method: :delete, class:'btn btn-outline-secondary', role:'button' %></li>
            <% else %>
              　<li class="nav-item"><%= link_to "ログイン", learner_session_path, class:'btn btn-outline-secondary', role:'button' %></li>
            <% end %>
          </ul>
        </div>
      </nav>

    </header>
    <%= yield %>
    <footer>

    </footer>
  </body>
</html>
